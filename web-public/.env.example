# R&A Labs public site (vite)
# Copy to .env.local and adjust. The API is served by the same origin in
# production (Vite proxy forwards /api to the backend in development).

# Where the customer portal lives (used by the AI Agent registration handoff).
# The public agent page links here with ?agent=<threadId> after a brief is saved.
VITE_CUSTOMER_PORTAL_URL=http://localhost:3004
