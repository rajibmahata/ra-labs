# Known bugs

Only real, reproducible bugs — not speculation ("this might break if...").
That goes in `technical-debt.md` instead. A stale entry here is worse than
none, so remove a row the moment it's actually fixed, don't just add a
"fixed" column.

| ID | Description | Severity | Repro steps | Found in |
|---|---|---|---|---|
| BUG-001 | Example: RAG retrieval returns another customer's chunk under X condition | Critical | steps here | M2 — delete this row |
