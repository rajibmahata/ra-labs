# Architecture decisions

One entry per non-obvious technical choice. If a future agent (or you, in
six months) would reasonably ask "why is it built this way," it belongs
here. Skip the obvious ones — this file is only useful if it stays short
enough to actually read.

---

## ADR-001: _Title_ — delete this example when the first real one is added

**Date:** YYYY-MM-DD
**Status:** proposed / accepted / superseded by ADR-00X

**Context:** what problem forced a decision.

**Decision:** what was chosen.

**Alternatives considered:** what else was on the table, and why it lost.

**Consequences:** what this makes easier, what it makes harder.
