# Completed features

Append-only log of what's actually done — cleared all six gates in
`WORKFLOW.md`, not just implemented. Newest at the top. Delete the example
row below when the first real entry is added.

| Date | Feature | Milestone | Notes |
|---|---|---|---|
| YYYY-MM-DD | Example: contact form + lead capture | M1 | Creates a `Lead` record, emails admin — delete this row |
