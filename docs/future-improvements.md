# Future improvements

Good ideas that are explicitly out of current scope — not technical debt
(nothing's being shortcut to defer these) and not a bug. This is where a
"we should also..." goes so it doesn't get lost, and so it doesn't derail
the current sprint either.

| Idea | Why it's not now | Revisit when |
|---|---|---|
| Example: online payment on invoices | Cash-only was the explicit M4 scope | After M4 ships — delete this row |
